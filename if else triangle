#include <stdio.h>

int main() {
    int x,y,z;
    printf("enter the values:");
    scanf("%d%d%d",&x,&y,&z);
    if("x==y&&y==z&&z==x")
    printf("equilateral triangle");
    else if(x==y||y==z||z==x)
    printf("isoceles triangle");
    else
    printf("scalen traingle");

    
    return 0;
}
